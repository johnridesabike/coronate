module Splash: {
  @react.component
  let make: unit => React.element
}

module TimeCalculator: {
  @react.component
  let make: unit => React.element
}

module NotFound: {
  @react.component
  let make: unit => React.element
}
